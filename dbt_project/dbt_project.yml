name: 'dbt_airbyte_poc'
version: '1.0.0'
config-version: 2

profile: 'dbt_airbyte_poc'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_airbyte_poc:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
    gold:
      +materialized: table
      +schema: gold
